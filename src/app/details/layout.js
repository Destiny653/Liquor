export const metadata = {
    title: "Premium Spirit Details",
    description: "Explore the rich heritage and unique tasting notes of our exclusive spirits. Certified authenticity for every bottle in our collection.",
};

export default function DetailsLayout({ children }) {
    return <>{children}</>;
}
